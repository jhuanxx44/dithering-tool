# AGENTS.md

仓库根目录的 agent 行为规则，适用于 Claude Code、Codex 等编码 agent。人类读者请看 [README.md](README.md)。

## 这是什么

零依赖、单文件的实时 1-bit 抖动渲染器（`<dither-light>` Web Component）。主线能力是**内置 SDF raymarch 的 3D 抖动**；接 Three.js、图片抖动、渐变背景是同一套 Bayer 管线的其他三种输入。

面向开源分享，坚持「小而美」：无构建步骤、无框架、无 npm 依赖。

## 硬约束

违反这几条会破坏项目定位，改动前先与用户确认：

1. **`dither-light.js` 必须保持单文件、零依赖、可直接 `<script src>` 引入。** 不要给它引入 npm 包、构建工具、CDN 依赖或外部字体。
2. **渲染引擎只有一份。** 所有渲染逻辑都在 `dither-light.js` 里；`index.html` 和 `examples/` 只做面板交互，不重复实现引擎。
3. **保持「打开即用」。** 任何改动都不应要求用户安装或联网。
4. **界面文案与文档用简体中文。**

### 零依赖的唯一例外

`examples/threejs-source.html` 允许从 CDN 引 Three.js。理由：「接任意 3D 模型」这个能力必须有可运行的对照页才讲得清楚，而 Three.js 无法内联。

- 例外**仅限这一个文件**，组件本体始终零依赖
- 该页面需在页面内和文档中标注「需要联网」
- 不要因为这个例外就给其他示例或组件加 CDN 依赖

## 改 3D 着色前必读

抖动只有「有墨 / 无墨」两级，可读性完全取决于**区域之间的密度差**，不是绝对亮度。这个坑很容易踩，且靠肉眼看截图不容易发现：

- 排成「背景最亮（几乎不出网点）→ 地面次之（稀疏）→ 造型暗面最暗（密集）」。内置场景当前实测 0% → 5% → 26%
- **背景要给亮**：`shadow` 预设里墨点跟着暗部走，深色背景会整片长满网点、把主体吞掉
- 造型亮面要**低于**背景亮度，否则轮廓溶进背景
- 不要把整幅压到中灰：会退化成满屏 50% 噪点，形体全丢
- 不要让亮部顶到 1.0：会变成一整片实心，同样丢形体

**改完必须用密度采样验证**（分区统计墨点占比），不要只靠看截图。

## 加 SDF 造型

往 `dither-light.js` 的 `SHAPES` 里加 `(px, py, pz, time) => 距离` 即可，渲染管线不用动。现成图元与算子：`sdSphere` / `sdBox` / `sdTorus` / `sdOctahedron`、`smoothUnion` / `smoothSubtract` / `rotY`。

新造型必须留在包围球 `BOUND_R`（1.55）内——它是性能优化的关键（排掉打向天空和远处地面的视线）。造型更大就同步调大这个常量，并重新测性能。

## 验证

改完代码必须实际跑一遍，不要只看 diff：

```bash
node --check dither-light.js
python3 -m http.server 8000    # 组件必须走 http://，file:// 下 canvas 会被污染
```

按改动范围检查：

| 改了什么 | 至少要验证 |
| --- | --- |
| 3D 着色 / 新造型 | 五种造型都能出形且互不相同；分区密度采样；全屏性能（`grid=4` 应在 ~11ms/帧 量级） |
| 组件属性 / API | 四种模式都不回归：3D、实时源、图片、渐变 |
| `index.html` / 示例页 | 浏览器打开确认渲染正常、控件可交互 |
| 视觉 / 交互 | 三种预设、光源切换、`prefers-reduced-motion`、移动端单列、高 DPI |
| UI 配色 | 文字对比度过 WCAG AA（4.5:1）。注意 3D 模式是**亮底**画面，浅色文字会看不见 |

效果有实质变化时重新截图 `preview.png`，否则不要动它。

### 环境注意

- **macOS 没有 `timeout` 命令**，headless 脚本不要依赖它，否则会静默失败得像代码报错
- headless 截图动画页面时不要用 `--virtual-time-budget`，rAF 循环在虚拟时间里不收敛，会挂住
- 清理 headless 进程时注意别杀掉用户自己的浏览器

## 文档同步

改动影响用户可见行为时，同步更新：

- `README.md`（英文，GitHub 默认展示）—— 属性表、API、参数建议
- `README.zh.md`（中文）—— **必须与英文版同步改**，两边内容等量。改了一边忘另一边是这个仓库最容易出的文档 bug
- `SKILL.md` —— agent 的发现入口，采用**渐进式加载**：本体只做意图分流（保持 100 行出头，上限 500 行），细节全部放 `references/`。frontmatter 必须是合法 YAML 且只用 `name` / `description`（Claude Code、Codex、`npx skills add` 三边通用，加专有字段会破坏通用性）
- `references/*.md` —— 按路径拆分的细节文档。新增能力时：先判断属于哪条路径（A 内置 3D / B 接模型 / C 图片 / D 背景），补到对应文件；只有跨路径的通用约定才动 `SKILL.md`
- `examples/` —— 保持最小、可直接拷走

## 提交

- **不擅自 `git commit` / `push`**，提交前先向用户确认
- 提交信息用中文，说明改动内容；涉及 LICENSE / README 的改动单独说明
- 修改只动必要的文件，保持 diff 最小
- 不在仓库里写 changelog 或对话记录（用 git 历史即可）
